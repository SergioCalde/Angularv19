# Maps App

Serve the app in development mode with `ng serve`.

1. Clone the repo
2. Install dependencies with `npm install`
3. Generate the `.env` file based on the `.env.template` file
4. Run `npm run set-envs`
5. Run `ng serve`